<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.everon.api.epit.mapper.SampleMapper">
    <select id="selectListSample" parameterMap="Map" resultType="Sample">
        SELECT 1 FROM DUAL
    </select>

    <select id="selectOneSample" parameterMap="Map" resultType="Sample">
        SELECT 1 FROM DUAL
    </select>
</mapper>